server:
  port: 8001
  servlet:
    context-path: /member
logging:
  level:
    ous.train.mapper: trace
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/train?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=UTC
    username: train
    password: train
mybatis:
  mapper-locations: classpath:mappers/*.xml
  type-aliases-package: ous.train.domain

